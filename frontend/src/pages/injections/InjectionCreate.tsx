import { Typography } from 'antd'

const { Title } = Typography

const InjectionCreate = () => {
  return (
    <div>
      <Title level={3}>创建故障注入</Title>
      <p>待实现可视化故障编排器...</p>
    </div>
  )
}

export default InjectionCreate
