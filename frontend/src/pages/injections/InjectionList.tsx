import { Typography } from 'antd'

const { Title } = Typography

const InjectionList = () => {
  return (
    <div>
      <Title level={3}>故障注入列表</Title>
      <p>待实现...</p>
    </div>
  )
}

export default InjectionList
