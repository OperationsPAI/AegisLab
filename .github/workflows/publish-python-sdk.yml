name: Publish Python SDK

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+*"
  workflow_dispatch:
    inputs:
      tag_version:
        description: "Version tag to simulate (e.g., v1.0.0)"
        required: true
        default: "v0.0.0"

permissions:
  actions: read
  contents: read
  packages: write
  id-token: write

jobs:
  pypi:
    name: Publish to PyPI
    runs-on: ubuntu-latest
    environment:
      name: pypi

    env:
      BLUE: "\033[1;34m"
      GREEN: "\033[1;32m"
      YELLOW: "\033[1;33m"
      RED: "\033[1;31m"
      CYAN: "\033[1;36m"
      GRAY: "\033[1;90m"
      RESET: "\033[0m"

      SEMVER_REGEX: "v[0-9]+.[0-9]+.[0-9]+*"

      SDK_DIR: "sdk/python"
      SDK_OPENAPI_DIR: "sdk/python/src/rcabench/openapi"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Extract Tag Version
        uses: ./.github/actions/extract-tag-version
        id: extract_version
        with:
          semver-regex: ${{ env.SEMVER_REGEX }}

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.23"

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Install dependencies
        run: |
          printf "${BLUE}📥 Installing Ruff...${RESET}\n"
          curl -LsSf https://astral.sh/ruff/install.sh | sh
          printf "${GREEN}✅ Ruff installed${RESET}\n"

          printf "${BLUE}📥 Installing Go dependencies...${RESET}\n"
          go install github.com/swaggo/swag/cmd/swag@latest
          (
            cd src && go mod tidy
          )
          printf "${GREEN}✅ Dependencies installed${RESET}\n"

      - name: Generate Python SDK
        run: |
          printf "${BLUE}🔧 Starting SDK generation via Makefile...${RESET}\n"

          make generate-python-sdk SDK_VERSION=${{ steps.extract_version.outputs.version }}

      - name: Verify SDK Generation
        run: |
          printf "${BLUE}🔍 Verifying generated SDK...${RESET}\n"

          if [ ! -d "${SDK_OPENAPI_DIR}" ]; then
            printf "${RED}❌ SDK openapi directory not found${RESET}\n"
            exit 1
          fi

          if [ ! -f "${SDK_DIR}/pyproject.toml" ]; then
            printf "${RED}❌ pyproject.toml not found${RESET}\n"
            exit 1
          fi

          printf "${GREEN}✅ SDK structure verified${RESET}\n"
          printf "${GRAY}SDK contents:${RESET}\n"
          ls -lh ${SDK_OPENAPI_DIR}

      - name: Build and publish
        run: |
          cd ${SDK_DIR}
          printf "${BLUE}🔨 Building SDK package...${RESET}\n"
          uv build

          printf "${BLUE}📦 Publishing to PyPI...${RESET}\n"
          uv publish --trusted-publishing always

          printf "${GREEN}✅ Successfully published to PyPI!${RESET}\n"
